spring:
  flyway:
    enabled: false
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: password
  jpa:
    database.platform: org.hibernate.dialect.H2Dialect
    show-sql: true
jc-api-key: SOME-KEY
slack-signing-secret: SOME-SECRET
slack-token: SOME-TOCKET
server:
  port: 80
allowed-channels: XYZ, ABC
base-command: /jc
domain: example.com
aws:
  secretName: prd-jumpcloud-slackbot
  region: ap-southeast-1

